import {Dialog as HeadlessDialog} from '@headlessui/react';
import styled from '@emotion/styled';

const Dialog = styled(HeadlessDialog)`
  position: relative;
  z-index: 10;
`;

export default Dialog;
